{
  "name": "yuanhao-personal-website",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/prepare-sites.mjs",
    "build:pages": "astro build && node scripts/prepare-github-pages.mjs",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "5.18.2"
  }
}
